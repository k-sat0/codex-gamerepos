# あそびの広場：制作時の共通設定

このリポジトリでゲーム・アプリを新規作成／変更するときは、以下を既定として適用する。ユーザーから別の指定がある場合は、その指定を優先する。

## 見た目とテーマ

- メインページと同じ SODA POP のライト／ダークテーマを使う。アプリごとに固定の配色へ切り替えない。
- 配色の正本はルートの `theme.css`。各アプリの CSS 冒頭で `@import url("../../theme.css");` として読み込み、色は共通 CSS 変数を参照する。色定義を複製しない。
- 背景 `--bg`、カード `--surface`、淡い背景 `--surface-soft`、文字 `--text`、補助文字 `--muted`、枠 `--border`、ボタン `--primary` / `--primary-hover` / `--on-primary`、黄 `--accent`、コーラル `--accent-strong` を使う。
- ライトの背景は `#eefbff`、ダークの背景は `#0d1b2a`。実際の値の変更は `theme.css` に集約する。
- 各 HTML の head でスタイルより前に共通 `../../theme.js` を読み込む。初回は OS 設定、以後は保存した選択を使う。
- ヘッダーに `data-theme-toggle` ボタン、その中に `data-theme-icon` と `data-theme-label` を配置する。テーマの保存キーは共通の `asobi-theme-v1` を継続使用する。
- Canvas を使う場合も CSS 変数から色を取得し、`data-theme` の変更時に再描画する。テーマ変更でゲームをリセットしない。
- 丸みのあるカード・ボタン、読みやすいコントラスト、キーボードのフォーカス表示を維持する。危険や成功など意味のあるゲーム内の色は、両テーマで識別できるようにする。

## 基本構成と操作

- HTML / CSS / JavaScript の静的サイト。依存ライブラリやビルド工程は必要がなければ追加しない。
- アプリは `apps/<app-name>/` に独立させ、相対パスを使う。GitHub Pages のリポジトリ配下およびローカルで動作させる。
- 各アプリに `../../index.html` へ戻るリンクを設け、メインページのカードと README の構成一覧を更新する。
- 日本語 UI、PC・スマホ対応。キーボード操作とタッチ操作を用意し、音は既定で OFF、`prefers-reduced-motion` に対応する。
- 保存キーはアプリ名＋バージョンで区別し、既存の記録は維持する。保存不可でもゲームを遊べるようにする。

## 確認と公開

- ライト／ダーク、テーマ切り替え中のゲーム状態維持、一覧との往復・再読込時のテーマ引継ぎ、スマホ幅、既存アプリへの影響を確認する。
- 偽造美術館のゲームロジック変更時は `node apps/forged-gallery/verify.cjs` で全5室のクリア可能性を確認する。
- 公開先は `https://k-sat0.github.io/codex-gamerepos/`。GitHub Pages は `main` ブランチのルートを配信する。
- 公開を依頼されたときは、対象の変更を確認してコミットし、`origin main` へ通常の push を行う。force push はしない。
- 公開完了は push だけで判断せず、GitHub Pages のビルド結果と公開 URL の更新を確認する。
